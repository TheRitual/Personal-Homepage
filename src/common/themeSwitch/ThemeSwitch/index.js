const ThemeSwitch = ({theme}) => {
    return (
        <div>
            <span>
                Theme: {theme.colors.shadeName}
            </span>
            <button />
        </div>
    );
}

export default ThemeSwitch;